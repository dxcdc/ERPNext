all:
  children:
    cdc_production:
      hosts:
        stok_production:
          # O dominio publico passa pela Cloudflare e nao encaminha SSH.
          ansible_host: 76.13.227.135
          ansible_user: root
